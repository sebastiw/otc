%% -*- erlang -*-
{deps, [{redbug, "2.0.8"}]}.

{plugins,
 [{provider_asn1, "0.3.0"}]}.

{asn1_args,
 [{compile_opts, [maps]}]}.

{provider_hooks,
 [{pre, [{compile, {asn, compile}}]},
  {post, [{clean, {asn, clean}}]}
 ]}.
